{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && cd showcase && pnpm install --frozen-lockfile && pnpm build"
  },
  "deploy": {
    "startCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && cd showcase && pnpm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
